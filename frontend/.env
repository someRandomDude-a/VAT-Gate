# Base URL for API calls. Point this to the hostname/port where the
# backend service is running. During local development using Docker
# Compose this will typically be `http://localhost:8080` because the
# Flask service exposes port 80 inside its container and the compose
# file maps it to port 8080 on the host.

VITE_API_BASE_URL=http://localhost:8080