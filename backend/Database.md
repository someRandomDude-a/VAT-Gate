Table containing session auth tokens and related user 
Table containing usernames and passwords (plain text for now to test)
Table containing all nodes. Each node has links to other nodes with their time
Table containing all package tokens (user is foreign key)
